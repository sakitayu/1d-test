# GitHub Personal Access Token (no scopes required for public repo search).
# Generate at: https://github.com/settings/tokens
# Required for: pnpm dev / pnpm build / pnpm start / pnpm test:e2e
# Not required for: pnpm test (vitest.setup.ts auto-sets a dummy)
GITHUB_TOKEN=
